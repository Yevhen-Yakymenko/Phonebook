import { LogoBox, LogoText, LogoIcon } from './Logo.styled';

const Logo = () => {
  return (
    <LogoBox>
      <LogoText>
        Phone <LogoIcon /> book
      </LogoText>
    </LogoBox>
  );
};

export default Logo;
